# Project Summary

The canonical project summary is maintained at:

```text
docs/PROJECT_SUMMARY.md
```

This pointer file exists for convenience because some workflows refer to
`PROJECT_SUMMARY.md` from the repository root, while the project-level
`AGENTS.md` requires the maintained memory document under `docs/`.

