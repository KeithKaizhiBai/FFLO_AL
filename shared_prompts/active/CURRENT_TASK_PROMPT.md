# Current Task Prompt

This file is the canonical active prompt for the current Codex task.

When the user says "use the shared prompt", "continue from prompt", or "read
active prompt", Codex should read this file first and treat it as the active
task instruction, while still respecting `AGENTS.md` and project memory files.

Current status:

```text
No active prompt has been selected yet.
```
