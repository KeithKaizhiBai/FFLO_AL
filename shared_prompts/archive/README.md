# Prompt Archive

This directory stores prompts that have already been executed, replaced, or
abandoned.

Do not overwrite archived prompts unless the user explicitly instructs Codex to
do so. Prefer creating a new timestamped archive file.
